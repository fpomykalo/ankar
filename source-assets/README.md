# Source assets

Alternative encodes kept out of `public/` so they are not deployed.

- `videos/hero-video-1-crf28.mp4`: hero video, H.264 CRF 28 at 1440px wide (5.6MB), the smaller option. The site serves the CRF 24 version (12.5MB) from `public/assets/videos/hero-video-1.mp4`.
